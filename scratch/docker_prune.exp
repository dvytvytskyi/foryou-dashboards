#!/usr/bin/expect -f
set password (PROD_SSH_PASSWORD)
spawn ssh -o StrictHostKeyChecking=no root@135.181.201.185 "docker system prune -f"
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
