#!/usr/bin/expect -f
set password (PROD_SSH_PASSWORD)
spawn ssh -o StrictHostKeyChecking=no root@135.181.201.185 "grep -r 'dashboards.foryou-realestate.com' /etc/nginx/"
expect "password:"
send "$password\r"
interact
